<UserControl x:Class="DbViewer.View.UpdateDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DbViewer.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel x:Name="stackPanel" Orientation="Vertical" 
                    HorizontalAlignment="Left">
            <Button x:Name="BackButton" Height="25" Width="25" Margin="5" 
                    BorderThickness="0" HorizontalAlignment="Left"
                    Click="BackButton_Click" Background="White">
                <Image Source="../Assets/arrow-left.png" Height="25"/>
            </Button>
            <Button x:Name="UpdateButton" Height="25"
                    BorderThickness="0" Background="#C3BEBE"
                    Click="UpdateButton_Click" Margin="5">
                <TextBlock Text="Обновить" Style="{StaticResource mainText}"/>
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
