<UserControl x:Class="DbViewer.View.AddDataPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DbViewer.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <ScrollViewer>
        <StackPanel x:Name="stackPanel" Orientation="Vertical" HorizontalAlignment="Left">
            <TextBlock Text="Выберите таблицу" Margin="5" 
                   Style="{StaticResource mainText}"/>
            <ComboBox x:Name="tables" Width="150" Margin="5" 
                      Style="{StaticResource mainComboBox}"/>
            <Button x:Name="AddButton" Height="25"
                    BorderThickness="0" Background="#C3BEBE"
                    Click="AddButton_Click" Margin="5">
                <TextBlock Text="Добавить" Style="{StaticResource mainText}"/>
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
